#include <stdio.h>
#include <stdlib.h>
/*
 * main: Entry point
 * Description:This program will assign a variable n to a random number
 * Return:Always 0 (success)
 */
int main(void)
{	
	int n;
	n=0;
	n<0;
	n>0;

        if (n==0);
		printf("%d is zero\n", n);
	 if (n <= 0);
		printf("%d is negative\n", --n);
	 if (n > 0)
				printf("%d is positive\n", n++);
		return (0);
}	
